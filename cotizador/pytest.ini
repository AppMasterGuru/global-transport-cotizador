[pytest]
# Only collect tests from the tests/ directory.
# Prevents pytest from crawling .venv, scripts/, and other directories
# that contain test_*.py files but are not part of the test suite.
testpaths = tests
norecursedirs = .venv venv .git __pycache__ scripts
